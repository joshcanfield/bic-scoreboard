name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Google Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Verify Gradle
        run: .\gradlew.bat --version

      - name: Run tests (incl. UI)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'
          .\gradlew.bat test --no-daemon --stacktrace --info *>&1 | Tee-Object -FilePath build\ci-test.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ github.run_id }}
          path: |
            build/reports/**
            build/test-results/**
            build/logs/**
            build/ci-test.log
          if-no-files-found: warn
          retention-days: 7

  package-app-image:
    needs: build-and-test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build app image zip
        run: .\gradlew.bat appImageZip --no-daemon --stacktrace

      - name: Sanitize ref name for artifact
        shell: pwsh
        run: |
          $safe = $env:GITHUB_REF_NAME -replace '/', '-'
          echo "REF_SAFE=$safe" >> $env:GITHUB_ENV

      - name: Upload app image artifact
        uses: actions/upload-artifact@v4
        with:
          name: scoreboard-app-image-${{ env.REF_SAFE }}-${{ github.sha }}
          path: build/artifacts/*.zip
          if-no-files-found: error
          retention-days: 14
